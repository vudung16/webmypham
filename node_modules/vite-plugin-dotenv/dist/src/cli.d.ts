import { Command } from "commander";
export interface Args {
    env: string;
    example: string;
    output: string[];
}
export declare const createCommand: () => Command;
export declare const resolve: ({ envFilePath, envExampleFilePath, }: {
    envFilePath: string;
    envExampleFilePath: string;
}) => Record<string, string>;
export declare const main: (di: {
    command: ReturnType<typeof createCommand>;
    resolve: typeof resolve;
}) => void;
